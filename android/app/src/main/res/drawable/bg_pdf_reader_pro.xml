<?xml version="1.0" encoding="UTF-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle" >

    <!-- beige, matched to the adaptive_pdf_reader plate edge tones: if a
         launcher ever shows the background layer at the mask edge it blends
         with the plate instead of showing a blue/green seam -->
    <gradient
        android:angle="90"
        android:startColor="#C9C8C6"
        android:endColor="#E9E2DB"
        android:type="linear" />

</shape>
